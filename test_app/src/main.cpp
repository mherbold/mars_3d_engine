// =============================================================================
// main.cpp
// MARS Test Application — Win32 entry point
//
// Responsibilities (to be implemented):
//   1. Create a Win32 window to render into.
//   2. Initialise the mars_engine (device, swap chain, renderer).
//   3. Load a small demo scene (e.g. a simple room or outdoor environment).
//   4. Run the main message loop, driving the path tracer each frame.
//   5. Handle keyboard / mouse input for the free-fly camera.
//   6. Shut down the engine cleanly on exit.
// =============================================================================

#include <windows.h>
#include <mars_engine/mars_engine.h>

// TODO: Implement WinMain
int WINAPI WinMain(
    _In_     HINSTANCE hInstance,
    _In_opt_ HINSTANCE hPrevInstance,
    _In_     LPSTR     lpCmdLine,
    _In_     int       nShowCmd)
{
    // TODO: Implement application startup, main loop, and shutdown.
    return 0;
}
